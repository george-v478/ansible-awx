- name: Collect host info
  hosts: all
  gather_facts: False
  
  tasks:

  - name: Run command and return hostname
    command: hostname
    register: local_out
    delegate_to: 10.0.2.5

  - debug: "msg={{ local_out }}"
